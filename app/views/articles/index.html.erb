<h1 id="logo">Kartoffel Posten</h1>

<% @articles.each do |article| %>
  <div class="articles">
    <h3><%= link_to article.title, article %></h3>
    <p><%= article.created_at %></p>

    <div><%= article.body %></div>
    <br>
  </div>
<% end %>
<%= link_to "New Post", new_article_path %>
